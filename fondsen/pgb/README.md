# `fondsen/pgb/` — PGB (placeholder)

Toekomstige home voor PGB-specifieke modules/overrides. **Nog geen productcode** — PGB-onboarding
staat achter gate G2 (besluit `0049`). Deze map bestaat nu alleen om de map­conventie en de
ESLint-boundary vast te leggen.

Regel: mag `core/*` gebruiken, niet `fondsen/horizon/*` of enig ander fonds.
